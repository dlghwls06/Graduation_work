const input = `
6번 조항: "계약 만기 전 임차인이 퇴실 시 새로운 임차인을 구하고 퇴실한다 (이때 관리비와 중개수수료를 부담한다)"

- 이유: 이 조항은 임차인에게 과도한 부담을 주고 있습니다.
- 추천 문장 1: "..."
- 추천 문장 2: "..."
- 추천 문장 3: "..."

7. 임차인은 잔금 지급 후 입주하기로 한다.

- 이유: 이 조항은 임차인에게 과도한 부담을 주고 있습니다.
- 추천 문장 1: "..."
- 추천 문장 2: "..."
- 추천 문장 3: "..."
`;

const regex = /(\d+)(?:번 조항)?:\s*"?(.+?)"?\s*\n- 이유:\s*(.*?)(?=\n\d+(?:번 조항)?:|\n\n|$)/gs;

const matches = [...input.matchAll(regex)];

console.log("🔥 추출된 수:", matches.length);
for (const m of matches) {
  console.log("번호:", m[1]);
  console.log("문장:", m[2]);
  console.log("이유:", m[3]);
}
